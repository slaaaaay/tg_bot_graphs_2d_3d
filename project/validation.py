def valid_a(a):
    if not a.isdigit():
        raise ValueError("переменная а должна быть числом")
    return a


def valid_b(b):
    if not b.isdigit():
        raise ValueError("переменная b должна быть числом")
    return b


def valid_c(c):
    if not c.isdigit():
        raise ValueError("переменная c должна быть числом")
    return c


def valid_k(k):
    if not k.isdigit():
        raise ValueError("переменная k должна быть числом")
    return k


def valid_a(self, a):
    if not a.isdigit():
        raise ValueError("переменная а должна быть числом")
    return a